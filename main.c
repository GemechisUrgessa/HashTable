/**
 * main.c 
 * 
 * This is our client code which uses the Employee ADT. 
 * to compile the Employee Abstract Data type example 
 * open a terminal  go to the 'abstract data type' folder, (using the command 'cd') then
 * 
 * gcc -m32 -o main main.c employee/employee.c 
 */
#include <stddef.h>
#include<assert.h>
//Notice here that we only include the header file which only contains the interfaces.
#include "./HashTable/hashTable.h" 


int main(int argc, char const *argv[])
{
    

    return 0;
}